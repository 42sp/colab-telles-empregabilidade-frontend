import {
	ConfigurationHeader,
	ConfigurationUploadArquivo,
} from "@/components/configuration";

const Configuration = () => {
	return (
		<div>
			<ConfigurationHeader />
			<ConfigurationUploadArquivo />
		</div>
	);
};

export default Configuration;
